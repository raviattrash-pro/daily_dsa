class Solution {
    public int minimumOneBitOperations(int n) {
        if (n == 0) return 0;
        int k = 0;
        int temp = n;
        while ((1 << (k + 1)) <= temp) k++; // find MSB position
        
        return (1 << (k + 1)) - 1 - minimumOneBitOperations(n ^ (1 << k));
    }
}
